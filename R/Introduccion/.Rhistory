install.packages("vscDebugger")
q()
print("Hola, mundo")
save.image("C:\\Users\\priet\\Desktop\\Estructuras de control\\R\\Introduccion\\.RData")
q()
load("~/.RData")
print("Hola mundo")
sumar <- function(a, b) {
resultado <- a + b
return(resultado)
}
# Definir una función para realizar una resta
restar <- function(a, b) {
resultado <- a - b
return(resultado)
}
# Definir una función para realizar una multiplicación
multiplicar <- function(a, b) {
resultado <- a * b
return(resultado)
}
# Definir una función para realizar una división
dividir <- function(a, b) {
if (b == 0) {
cat("No se puede dividir por cero.\n")
return(NULL)
} else {
resultado <- a / b
return(resultado)
}
}
# Solicitar al usuario que ingrese los números y la operación
cat("Calculadora en R\n")
num1 <- as.numeric(readline("Ingresa el primer número: "))
operacion <- readline("Ingresa la operación (+, -, *, /): ")
# Realizar la operación seleccionada
if (operacion == "+") {
resultado <- sumar(num1, num2)
} else if (operacion == "-") {
resultado <- restar(num1, num2)
} else if (operacion == "*") {
resultado <- multiplicar(num1, num2)
} else if (operacion == "/") {
resultado <- dividir(num1, num2)
} else {
cat("Operación no válida.\n")
resultado <- NULL
}
# Mostrar el resultado
if (!is.null(resultado)) {
cat("Resultado:", resultado, "\n")
}
sumar <- function(a, b) {
  resultado <- a + b
  return(resultado)
}
# Definir una función para realizar una resta
restar <- function(a, b) {
  resultado <- a - b
  return(resultado)
}
# Definir una función para realizar una multiplicación
multiplicar <- function(a, b) {
  resultado <- a * b
  return(resultado)
}
# Definir una función para realizar una división
dividir <- function(a, b) {
  if (b == 0) {
    cat("No se puede dividir por cero.\n")
    return(NULL)
  } else {
    resultado <- a / b
    return(resultado)
  }
}
# Solicitar al usuario que ingrese los números y la operación
cat("Calculadora en R\n")
num1 <- as.numeric(readline("Ingresa el primer número: "))
operacion <- readline("Ingresa la operación (+, -, *, /): ")
# Realizar la operación seleccionada
if (operacion == "+") {
  resultado <- sumar(num1, num2)
} else if (operacion == "-") {
  resultado <- restar(num1, num2)
} else if (operacion == "*") {
  resultado <- multiplicar(num1, num2)
} else if (operacion == "/") {
  resultado <- dividir(num1, num2)
} else {
  cat("Operación no válida.\n")
  resultado <- NULL
}
# Mostrar el resultado
if (!is.null(resultado)) {
  cat("Resultado:", resultado, "\n")
}
